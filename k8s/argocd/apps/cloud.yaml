apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloud
  namespace: argocd
  labels:
    homelab/stack: cloud
spec:
  project: default
  source:
    repoURL: ${GITHUB_REPO}
    targetRevision: main
    path: k8s/stacks/cloud
    plugin:
      name: kustomize-envsubst
  destination:
    server: https://kubernetes.default.svc
    namespace: cloud
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

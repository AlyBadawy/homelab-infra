apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Non-Helm monitor resources managed by ArgoCD (infra-monitor-config app).
# The kube-prometheus-stack Helm chart is managed by the infra-monitor app.
resources:
  - ingress.yaml
  - longhorn-pvcs.yaml
